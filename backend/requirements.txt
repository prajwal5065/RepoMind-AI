# ── Production dependencies ────────────────────────────────────────────────
# Pin exact versions to prevent supply-chain drift.
# To upgrade: update version, run pip-audit, then re-test.
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.2.2
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.31
faiss-cpu==1.9.0
google-genai==1.9.0
openai==1.59.3
python-json-logger==2.0.7
# python-jose was removed: it is not imported anywhere in this codebase
# (grep found zero usages) and pip-audit flagged CVEs in both it and its
# ecdsa transitive dependency, the latter with no fix version available.
# If JWT auth is added later per SECURITY_AUDIT.md's low-priority
# recommendation, re-add it then and re-run pip-audit at that version.

# ── Development / CI only — DO NOT install in production Docker images ──────
# pip install -r requirements-dev.txt
# pylint
# flake8
# mypy
# bandit
# semgrep
# pytest
# httpx
