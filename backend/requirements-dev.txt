# ── Development / CI tooling ────────────────────────────────────────────
# Referenced from requirements.txt but never actually committed until now.
# Install with: pip install -r requirements-dev.txt
# Do NOT install these in production Docker images.
pylint==3.3.1
flake8==7.1.1
mypy==1.13.0
bandit==1.7.10
pytest==8.3.3
httpx==0.27.2
pip-audit==2.7.3
